# Site Overrides

These markdown files were extracted from the TVL-branded overrides inside the
ignored `intelligent-textbooks` clone.

They are preserved as source notes because they may contain reusable landing-page
copy, but they are not yet declared as canonical book chapters.
